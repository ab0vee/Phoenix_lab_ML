# 🚀 Быстрый старт с Git

## 📍 Где выполнять команды

**Все команды выполняются в корневой папке проекта:**
```
D:\Phoenix LAB
```

### Как открыть терминал в нужной папке:

**Вариант 1: PowerShell/CMD**
1. Откройте Проводник Windows
2. Перейдите в папку `D:\Phoenix LAB`
3. В адресной строке введите `powershell` или `cmd` и нажмите Enter
4. Терминал откроется в этой папке

**Вариант 2: Через меню**
1. Откройте папку `D:\Phoenix LAB` в Проводнике
2. Щелкните правой кнопкой на свободном месте
3. Выберите "Открыть в терминале" (Windows 11) или "Открыть окно PowerShell здесь"

**Вариант 3: Через VS Code/Cursor**
- Откройте папку проекта в редакторе
- Откройте встроенный терминал (`` Ctrl+` `` или `Terminal > New Terminal`)
- Терминал автоматически откроется в корне проекта

---

## 📋 Пошаговая инструкция

### Шаг 1: Проверьте текущую папку

```powershell
# Вы должны увидеть путь: D:\Phoenix LAB
pwd
# или
Get-Location
```

Если вы не в правильной папке, перейдите туда:
```powershell
cd "D:\Phoenix LAB"
```

---

### Шаг 2: Инициализируйте Git (если еще не сделано)

```powershell
git init
```

Вы увидите: `Initialized empty Git repository in D:/Phoenix LAB/.git/`

---

### Шаг 3: Проверьте, что будет добавлено

```powershell
git status
```

**Что должно быть:**
- ✅ Зеленым: файлы, которые будут добавлены (код, документация)
- ✅ Красным (ignored): файлы, которые будут исключены (models_cache, node_modules, .env)

**Если видите models_cache или .env в списке для добавления - проверьте .gitignore!**

---

### Шаг 4: Добавьте все файлы

```powershell
git add .
```

Команда добавит все файлы, кроме тех, что в .gitignore

---

### Шаг 5: Создайте первый коммит

```powershell
git commit -m "Initial commit: Phoenix LAB project"
```

---

### Шаг 6: Добавьте удаленный репозиторий

**Если используете GitHub:**
1. Создайте новый репозиторий на GitHub (без README, без .gitignore)
2. Скопируйте URL репозитория (например: `https://github.com/username/phoenix-lab.git`)

```powershell
git remote add origin https://github.com/ваш-username/название-репозитория.git
```

**Если используете GitLab:**
```powershell
git remote add origin https://gitlab.com/ваш-username/название-репозитория.git
```

---

### Шаг 7: Переименуйте ветку (опционально, если нужно)

```powershell
git branch -M main
```

---

### Шаг 8: Отправьте в репозиторий

```powershell
git push -u origin main
```

Введите логин и пароль (или используйте токен доступа для GitHub)

---

## ✅ Проверка после пуша

После успешного пуша проверьте на GitHub/GitLab:
- ✅ Все файлы на месте
- ❌ НЕТ папки `models_cache/`
- ❌ НЕТ папки `node_modules/`
- ❌ НЕТ файла `.env`
- ❌ НЕТ файлов `*.env` в `backend/rewrite_service/`

---

## 🆘 Если что-то пошло не так

### Ошибка: "fatal: not a git repository"
**Решение:** Вы не в корневой папке проекта. Выполните:
```powershell
cd "D:\Phoenix LAB"
git init
```

### Ошибка: "fatal: remote origin already exists"
**Решение:** Удалите существующий remote и добавьте снова:
```powershell
git remote remove origin
git remote add origin <ваш-url>
```

### Случайно добавили большой файл
**Решение:** Удалите его из Git (но оставьте на диске):
```powershell
git rm -r --cached backend/ml_service/models_cache/
git commit -m "Remove models_cache from git"
git push
```

---

## 📊 Ожидаемый результат

После успешного пуша:
- **Размер репозитория:** ~5-10 MB
- **Количество файлов:** ~200-500 файлов (только код)
- **Время загрузки:** 1-5 минут (зависит от скорости интернета)

---

## 💡 Полезные команды

```powershell
# Посмотреть, какие файлы будут добавлены
git status

# Посмотреть список добавленных файлов
git ls-files

# Проверить размер файлов, которые будут добавлены
git ls-files | ForEach-Object { Get-Item $_ } | 
  Sort-Object Length -Descending | 
  Select-Object -First 20 Name, @{N="Size(MB)";E={[math]::Round($_.Length/1MB,2)}}

# Проверить, что файл исключен
git check-ignore -v backend/ml_service/models_cache

# Посмотреть историю коммитов
git log --oneline
```

---

## ✅ Готово!

Теперь ваш проект в Git, и вы можете безопасно делиться им с другими!

