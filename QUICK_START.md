# 🚀 Быстрый старт Phoenix LAB

## Запуск через Docker Compose (рекомендуется)

### 1. Подготовка

```bash
# Скопируйте пример файла окружения
cp .env.example .env

# Отредактируйте .env и заполните необходимые API ключи
# Минимально необходимые:
# - API_KEY (для ML Service)
# - OPENROUTER_API_KEY (для Qwen модели)
# - YANDEX_CLOUD_API_KEY (для Yandex Alice модели)
```

### 2. Запуск всех сервисов

**Windows:**
```bash
docker-start.bat
```

**Linux/Mac:**
```bash
chmod +x docker-start.sh
./docker-start.sh
```

**Или вручную:**
```bash
docker-compose up -d
```

### 3. Проверка статуса

```bash
docker-compose ps
```

Все сервисы должны быть в статусе `Up` и `healthy`.

### 4. Откройте в браузере

- **Frontend:** http://localhost:3000
- **API Docs (ML Service):** http://localhost:8000/docs
- **API Health (Rewrite Service):** http://localhost:5000/api/health

## Остановка

```bash
docker-compose down
```

## Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f frontend
docker-compose logs -f rewrite_service
docker-compose logs -f ml_service
```

## Пересборка после изменений

```bash
# Все сервисы
docker-compose up -d --build

# Конкретный сервис
docker-compose up -d --build frontend
```

## Структура сервисов

| Сервис | Порт | Описание |
|--------|------|----------|
| frontend | 3000 | Next.js веб-интерфейс |
| rewrite_service | 5000 | Flask API для рерайта |
| ml_service | 8000 | FastAPI для ML моделей |
| telegram_bot | - | Telegram бот |
| postgres | 5432 | PostgreSQL БД |
| redis | 6379 | Redis кэш |

## Минимальные требования

- Docker и Docker Compose
- 8 GB RAM (для ML моделей)
- 10 GB свободного места на диске
- Интернет (для загрузки моделей при первом запуске)

## Первый запуск

При первом запуске ML Service автоматически скачает модели (~2-3 GB). Это может занять 10-30 минут в зависимости от скорости интернета.

Модели кэшируются в Docker volume, поэтому при последующих запусках загрузка не требуется.

## Troubleshooting

### Порт занят

Если порт занят, измените его в `docker-compose.yml`:

```yaml
ports:
  - "3001:3000"  # Вместо 3000:3000
```

### Модели не загружаются

1. Проверьте интернет-соединение
2. Проверьте логи: `docker-compose logs ml_service`
3. Убедитесь, что достаточно места на диске

### Frontend не подключается к API

1. Убедитесь, что все сервисы запущены: `docker-compose ps`
2. Проверьте переменные окружения в `docker-compose.yml`
3. Проверьте логи: `docker-compose logs frontend rewrite_service`



