# 🔐 Решение проблемы авторизации Git (403 Forbidden)

## ❌ Проблема

```
remote: Permission to ab0vee/Phoenix_lab_ML.git denied to INSOLENTik.
fatal: unable to access 'https://github.com/ab0vee/Phoenix_lab_ML/': The requested URL returned error: 403
```

### Что это значит:
- Вы пытаетесь запушить в репозиторий `ab0vee/Phoenix_lab_ML`
- Ваш GitHub аккаунт: `INSOLENTik`
- У вас нет прав на запись в этот репозиторий

---

## ✅ Решения

### Вариант 1: Это ВАШ репозиторий (неправильные учетные данные)

Если репозиторий `ab0vee/Phoenix_lab_ML` - ваш, но под другим аккаунтом:

#### Решение 1.1: Использовать Personal Access Token

1. **Создайте Personal Access Token:**
   - Откройте: https://github.com/settings/tokens
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Название: `Phoenix Lab Push`
   - Срок действия: выберите нужный (например, 90 дней или "No expiration")
   - Права (scopes): отметьте `repo` (полный доступ к репозиториям)
   - Нажмите "Generate token"
   - **Скопируйте токен** (показывается только один раз!)

2. **Используйте токен вместо пароля:**
   ```powershell
   # При следующем push Git попросит логин и пароль
   # Логин: ваш GitHub username (ab0vee)
   # Пароль: вставьте токен (не обычный пароль!)
   git push -u origin main
   ```

3. **Или сохраните токен в Git Credential Manager:**
   ```powershell
   # Удалите старые учетные данные
   git credential-manager erase https://github.com
   
   # При следующем push введите токен как пароль
   git push -u origin main
   ```

#### Решение 1.2: Использовать SSH (рекомендуется)

1. **Проверьте, есть ли SSH ключ:**
   ```powershell
   ls ~/.ssh/id_*.pub
   ```

2. **Если нет - создайте:**
   ```powershell
   ssh-keygen -t ed25519 -C "your_email@example.com"
   # Нажимайте Enter для всех вопросов (или введите пароль)
   ```

3. **Скопируйте публичный ключ:**
   ```powershell
   cat ~/.ssh/id_ed25519.pub
   # Скопируйте весь вывод
   ```

4. **Добавьте ключ в GitHub:**
   - Откройте: https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Title: `My Windows PC`
   - Key: вставьте скопированный ключ
   - Нажмите "Add SSH key"

5. **Измените remote на SSH:**
   ```powershell
   git remote set-url origin git@github.com:ab0vee/Phoenix_lab_ML.git
   git push -u origin main
   ```

---

### Вариант 2: Это НЕ ваш репозиторий (нужен свой)

Если репозиторий `ab0vee/Phoenix_lab_ML` принадлежит другому пользователю:

#### Решение 2.1: Создать свой репозиторий

1. **Создайте новый репозиторий на GitHub:**
   - Откройте: https://github.com/new
   - Repository name: `Phoenix-LAB` (или любое другое)
   - Description: `Phoenix LAB - ML Service for article rewriting`
   - Visibility: Private или Public (на ваше усмотрение)
   - **НЕ добавляйте** README, .gitignore, license
   - Нажмите "Create repository"

2. **Измените remote на ваш репозиторий:**
   ```powershell
   git remote remove origin
   git remote add origin https://github.com/INSOLENTik/название-вашего-репозитория.git
   git push -u origin main
   ```

   Или через SSH:
   ```powershell
   git remote remove origin
   git remote add origin git@github.com:INSOLENTik/название-вашего-репозитория.git
   git push -u origin main
   ```

---

### Вариант 3: Исправить учетные данные

Если вы точно знаете, что репозиторий ваш:

#### Решение 3.1: Очистить кэш учетных данных

**Windows:**
```powershell
# Удалить сохраненные учетные данные
git credential-manager erase https://github.com

# Или через Windows Credential Manager:
# Win + R → control /name Microsoft.CredentialManager
# Удалите записи для github.com
```

#### Решение 3.2: Использовать правильный аккаунт

Если вы залогинены под неправильным аккаунтом в браузере:
1. Выйдите из GitHub в браузере
2. При push Git попросит войти заново
3. Войдите под правильным аккаунтом (ab0vee)

---

## 🔍 Проверка текущих настроек

```powershell
# Посмотреть, какой remote настроен
git remote -v

# Посмотреть, какой пользователь используется
git config user.name
git config user.email
```

---

## ✅ Рекомендуемый порядок действий

1. **Определите, ваш ли это репозиторий:**
   - Если `ab0vee` - ваш аккаунт → используйте Вариант 1
   - Если `ab0vee` - не ваш аккаунт → используйте Вариант 2

2. **Создайте Personal Access Token** (если используете HTTPS):
   - Безопаснее и удобнее, чем пароль
   - Следуйте Решению 1.1

3. **Или настройте SSH** (рекомендуется):
   - Один раз настроить, потом работает автоматически
   - Следуйте Решению 1.2

---

## 🆘 Частые вопросы

**Q: Как узнать, какой у меня аккаунт GitHub?**  
A: Откройте https://github.com - в правом верхнем углу будет ваш username.

**Q: Какой способ лучше - HTTPS или SSH?**  
A: SSH удобнее (не нужно вводить пароль каждый раз), но HTTPS проще настроить.

**Q: Токен показывает только один раз?**  
A: Да, поэтому сохраните его в надежном месте. Если потеряете - создайте новый.

**Q: Можно ли использовать один токен для нескольких репозиториев?**  
A: Да, один токен дает доступ ко всем вашим репозиториям.

---

## 📝 Быстрая инструкция (Personal Access Token)

```powershell
# 1. Создайте токен на https://github.com/settings/tokens
# 2. Удалите старые учетные данные
git credential-manager erase https://github.com

# 3. Попробуйте снова
git push -u origin main

# При запросе:
# Username: ваш-github-username
# Password: вставьте токен (не пароль!)
```

---

## ✅ После успешного пуша

Проверьте на GitHub:
- Все файлы загружены
- Нет `models_cache/`, `node_modules/`, `.env`
- Размер репозитория ~5-10 MB

Удачи! 🚀

