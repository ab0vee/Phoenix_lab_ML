# Как передать проект другому человеку

Проект содержит большие файлы (модели ML, зависимости), поэтому передавать весь проект целиком неэффективно.

## 📦 Что НЕ нужно передавать

Следующие директории **НЕ должны** быть в архиве:
- `backend/ml_service/models_cache/` - модели скачиваются автоматически при первом запуске
- `frontend/node_modules/` - устанавливаются через `npm install`
- `backend/rewrite_service/__pycache__/` - кэш Python
- `.next/` - кэш Next.js
- `*.log` - логи
- `.env` - переменные окружения (создается отдельно)

## ✅ Способ 1: Git (рекомендуется)

### Для отправителя:

1. Убедитесь, что `.gitignore` настроен правильно (уже настроен)
2. Проверьте, что большие файлы не в репозитории:
   ```bash
   git status
   ```
3. Если нужно, удалите большие файлы из истории:
   ```bash
   git rm -r --cached backend/ml_service/models_cache/
   git rm -r --cached frontend/node_modules/
   git commit -m "Remove large files from git"
   ```
4. Запушьте в репозиторий (GitHub, GitLab, etc.)

### Для получателя:

1. Клонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd "Phoenix LAB"
   ```
2. Создайте файл `.env` (см. раздел "Настройка окружения")
3. Запустите через Docker Compose:
   ```bash
   docker-compose up -d --build
   ```
4. Модели скачаются автоматически при первом запуске ML Service (~2-3 GB, 10-30 минут)

---

## ✅ Способ 2: Архив без больших файлов

### Для отправителя:

1. Создайте архив, исключив большие директории:

   **Windows (PowerShell):**
   ```powershell
   # Создаем список файлов для архивации (исключая большие директории)
   $exclude = @('backend\ml_service\models_cache', 'frontend\node_modules', '.next', '__pycache__', '*.log', '.env')
   Get-ChildItem -Path . -Recurse -File | 
     Where-Object { 
       $path = $_.FullName
       -not ($exclude | Where-Object { $path -like "*$_*" })
     } | 
     Compress-Archive -DestinationPath "Phoenix-LAB-Project.zip"
   ```

   **Или используйте 7-Zip/WinRAR:**
   - Выберите все файлы проекта
   - Исключите: `backend/ml_service/models_cache/`, `frontend/node_modules/`, `.next/`, `__pycache__/`, `*.log`, `.env`

2. Передайте архив получателю (через облако, USB, etc.)

### Для получателя:

1. Распакуйте архив
2. Создайте файл `.env` (см. раздел "Настройка окружения")
3. Запустите через Docker Compose:
   ```bash
   docker-compose up -d --build
   ```
4. Модели скачаются автоматически при первом запуске

---

## ✅ Способ 3: Docker образы (для готового окружения)

Если нужно передать проект с уже скачанными моделями:

### Для отправителя:

1. Сохраните Docker образы:
   ```bash
   docker save phoenixlab-ml_service:latest -o ml_service.tar
   docker save phoenixlab-rewrite_service:latest -o rewrite_service.tar
   docker save phoenixlab-frontend:latest -o frontend.tar
   docker save postgres:15 -o postgres.tar
   ```

2. Создайте архив с кодом (без `models_cache` и `node_modules`) и образами:
   ```bash
   # Архивируйте код (исключая большие директории)
   # Добавьте .tar файлы образов
   ```

3. Передайте получателю

### Для получателя:

1. Распакуйте архив
2. Загрузите образы:
   ```bash
   docker load -i ml_service.tar
   docker load -i rewrite_service.tar
   docker load -i frontend.tar
   docker load -i postgres.tar
   ```
3. Создайте `.env` файл
4. Запустите:
   ```bash
   docker-compose up -d
   ```

**⚠️ Внимание:** Docker образы могут быть очень большими (5-10 GB), поэтому этот способ подходит только для локальной передачи (USB, локальная сеть).

---

## 🔧 Настройка окружения

### Создание `.env` файла

Получатель должен создать файл `.env` в корне проекта со следующим содержимым:

```env
# Database
POSTGRES_USER=phoenix_user
POSTGRES_PASSWORD=phoenix_password
POSTGRES_DB=phoenix_db
DATABASE_URL=postgresql://phoenix_user:phoenix_password@postgres:5432/phoenix_db

# API Keys (получить у отправителя или создать новые)
OPENROUTER_API_KEY=your_openrouter_key_here
YANDEX_CLOUD_API_KEY=your_yandex_key_here
YANDEX_CLOUD_ASSISTANT_ID=your_assistant_id_here
KANDINSKY_API_KEY=your_kandinsky_key_here
KANDINSKY_SECRET_KEY=your_kandinsky_secret_here
PEXELS_API_KEY=your_pexels_key_here
UNSPLASH_ACCESS_KEY=your_unsplash_key_here

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# API Keys для сервисов
API_KEY=your_api_key_here

# URLs (обычно не нужно менять)
NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_ML_SERVICE_URL=http://localhost:8000
```

**Важно:** Отправитель должен передать получателю:
- Значения всех API ключей (или инструкции по их получению)
- Токен Telegram-бота (если используется)

---

## 📋 Чек-лист для передачи проекта

### Отправитель должен:
- [ ] Проверить, что `.gitignore` настроен правильно
- [ ] Убедиться, что большие файлы не включены в архив/Git
- [ ] Создать файл `.env.example` с примерами (без реальных ключей)
- [ ] Подготовить список необходимых API ключей
- [ ] Создать инструкцию по получению API ключей (если нужно)
- [ ] Передать проект (Git/архив/Docker образы)

### Получатель должен:
- [ ] Получить проект (клонировать/распаковать)
- [ ] Создать файл `.env` с реальными ключами
- [ ] Установить Docker и Docker Compose
- [ ] Запустить `docker-compose up -d --build`
- [ ] Дождаться скачивания моделей (10-30 минут при первом запуске)
- [ ] Проверить работу: открыть http://localhost:3000

---

## 🚀 Быстрый старт для получателя

```bash
# 1. Получить проект (Git)
git clone <repository-url>
cd "Phoenix LAB"

# 2. Создать .env файл
cp .env.example .env
# Отредактировать .env и добавить реальные ключи

# 3. Запустить
docker-compose up -d --build

# 4. Проверить логи
docker-compose logs -f

# 5. Открыть в браузере
# http://localhost:3000
```

---

## 📊 Размеры проекта

- **Код проекта (без моделей и зависимостей):** ~5-10 MB
- **С зависимостями (node_modules):** ~200-300 MB
- **С моделями ML:** ~3-5 GB
- **Docker образы:** ~5-10 GB

**Рекомендация:** Передавайте только код (без моделей и зависимостей). Модели скачаются автоматически при первом запуске.

---

## ❓ Частые вопросы

**Q: Почему модели не передаются?**  
A: Модели весят 2-3 GB и скачиваются автоматически при первом запуске. Это быстрее и удобнее, чем передавать их вручную.

**Q: Как долго скачиваются модели?**  
A: При первом запуске ML Service скачает модели за 10-30 минут (зависит от скорости интернета).

**Q: Можно ли передать модели отдельно?**  
A: Да, можно скопировать `backend/ml_service/models_cache/` отдельно, но это не обязательно - модели скачаются автоматически.

**Q: Нужно ли передавать Docker образы?**  
A: Нет, образы собираются автоматически из Dockerfile при запуске `docker-compose up --build`.

---

## 📞 Поддержка

Если возникли проблемы при передаче проекта, проверьте:
1. Все ли необходимые файлы включены
2. Правильно ли настроен `.env`
3. Установлены ли Docker и Docker Compose
4. Доступен ли интернет для скачивания моделей

