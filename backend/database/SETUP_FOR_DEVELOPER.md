# 🛠️ Инструкция для разработчика БД

Этот документ для **того, кто будет создавать и настраивать БД**.

## 📋 Задачи

1. Создать схему БД (таблицы, индексы, триггеры)
2. Подготовить дамп БД для передачи
3. Предоставить параметры подключения

## 🚀 Шаг 1: Установка PostgreSQL

### Windows
```powershell
# Через Chocolatey
choco install postgresql

# Или скачайте установщик с https://www.postgresql.org/download/windows/
```

### Linux (Ubuntu/Debian)
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
```

### macOS
```bash
brew install postgresql
brew services start postgresql
```

### Docker (рекомендуется)
```bash
docker run -d \
  --name phoenix_postgres \
  -e POSTGRES_USER=phoenix_user \
  -e POSTGRES_PASSWORD=your_password \
  -e POSTGRES_DB=phoenix_lab \
  -p 5432:5432 \
  -v postgres_data:/var/lib/postgresql/data \
  postgres:15-alpine
```

## 📝 Шаг 2: Создание БД и схемы

### Вариант A: Использование готового schema.sql

```bash
# 1. Создайте базу данных
createdb phoenix_lab

# 2. Выполните схему
psql -d phoenix_lab -f backend/database/schema.sql
```

### Вариант B: Через psql интерактивно

```bash
psql -U postgres

-- В psql:
CREATE DATABASE phoenix_lab;
\c phoenix_lab
\i backend/database/schema.sql
\q
```

### Вариант C: Через Docker

```bash
# Если PostgreSQL в Docker
docker exec -i phoenix_postgres psql -U phoenix_user -d phoenix_lab < backend/database/schema.sql
```

## ✅ Шаг 3: Проверка создания

```sql
-- Подключитесь к БД
psql -d phoenix_lab

-- Проверьте таблицы
\dt

-- Должны быть:
-- users
-- user_urls
-- url_processing_results
-- user_sessions

-- Проверьте структуру
\d users
\d user_urls
```

## 📦 Шаг 4: Создание дампа для передачи

### Создание SQL дампа (текстовый формат)

```bash
# Полный дамп (схема + данные)
pg_dump -U phoenix_user -d phoenix_lab > phoenix_lab_dump.sql

# Только схема (без данных)
pg_dump -U phoenix_user -d phoenix_lab --schema-only > phoenix_lab_schema.sql

# Только данные (без схемы)
pg_dump -U phoenix_user -d phoenix_lab --data-only > phoenix_lab_data.sql
```

### Создание бинарного дампа (более компактный)

```bash
# Бинарный формат (можно сжать)
pg_dump -U phoenix_user -d phoenix_lab -F c -f phoenix_lab.dump

# Восстановление:
pg_restore -U phoenix_user -d phoenix_lab phoenix_lab.dump
```

### Через Docker

```bash
# Создание дампа
docker exec phoenix_postgres pg_dump -U phoenix_user phoenix_lab > phoenix_lab_dump.sql

# Или бинарный
docker exec phoenix_postgres pg_dump -U phoenix_user -F c phoenix_lab > phoenix_lab.dump
```

## 📋 Шаг 5: Подготовка документации для передачи

Создайте файл `DATABASE_INFO.md` со следующей информацией:

```markdown
# Информация о базе данных

## Параметры подключения
- **Хост**: localhost (или IP адрес)
- **Порт**: 5432
- **База данных**: phoenix_lab
- **Пользователь**: phoenix_user
- **Пароль**: [укажите пароль]

## Строка подключения
postgresql://phoenix_user:password@localhost:5432/phoenix_lab

## Версия PostgreSQL
PostgreSQL 15.x

## Файлы для передачи
1. phoenix_lab_dump.sql - полный дамп БД
2. schema.sql - только схема (для справки)

## Структура
- Таблица users: пользователи системы
- Таблица user_urls: ссылки пользователей
- Таблица url_processing_results: результаты обработки
- Таблица user_sessions: сессии пользователей

## Примечания
- Все пароли хранятся в виде хешей
- Используется автоматическое обновление updated_at
- Есть индексы для оптимизации запросов
```

## 🧪 Шаг 6: Тестовые данные (опционально)

Если нужно добавить тестовые данные для проверки:

```sql
-- Вставка тестового пользователя
INSERT INTO users (username, email, password_hash, full_name)
VALUES 
    ('test_user', 'test@example.com', '$2b$12$...', 'Test User'),
    ('admin', 'admin@example.com', '$2b$12$...', 'Admin User');

-- Вставка тестовых ссылок
INSERT INTO user_urls (user_id, url, title, status)
VALUES 
    (1, 'https://example.com/article1', 'Test Article 1', 'active'),
    (1, 'https://example.com/article2', 'Test Article 2', 'active');
```

**⚠️ ВАЖНО**: Не передавайте тестовые данные с реальными паролями!

## 📤 Шаг 7: Передача файлов

Передайте следующему разработчику:

1. ✅ `phoenix_lab_dump.sql` - дамп БД
2. ✅ `schema.sql` - схема (для справки)
3. ✅ `DATABASE_INFO.md` - информация о подключении
4. ✅ (Опционально) `backend/database/` - вся папка с миграциями

### Способы передачи:

- **Git**: Закоммитьте в репозиторий (⚠️ без паролей!)
- **Архив**: Создайте ZIP с файлами
- **Облако**: Google Drive, Dropbox и т.д.
- **Прямая передача**: USB, сеть

## 🔐 Безопасность

⚠️ **НИКОГДА не передавайте:**
- Пароли в открытом виде
- Production данные с реальными пользователями
- Чувствительную информацию

✅ **Можно передавать:**
- SQL дампы (без паролей)
- Схему БД
- Тестовые данные (без реальных паролей)

## ✅ Чеклист перед передачей

- [ ] Схема БД создана и протестирована
- [ ] Все таблицы созданы (`\dt` показывает все таблицы)
- [ ] Индексы созданы (`\di` показывает индексы)
- [ ] Создан дамп БД
- [ ] Дамп проверен (можно восстановить на другой машине)
- [ ] Подготовлена документация с параметрами подключения
- [ ] Удалены все пароли и чувствительные данные из документации
- [ ] Файлы готовы к передаче

## 🆘 Помощь

Если возникли проблемы:

1. Проверьте логи PostgreSQL
2. Убедитесь, что PostgreSQL запущен
3. Проверьте права доступа пользователя
4. Убедитесь, что порт 5432 не занят

